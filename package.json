{
  "name": "@them-codes/either",
  "version": "1.0.13",
  "description": "Functional exception handling",
  "main": "either.js",
  "types": "either.d.ts",
  "files": [
    "either.js",
    "either.d.ts"
  ],
  "devDependencies": {
    "standard-version": "^9.5.0"
  },
  "scripts": {
    "publish": "standard-version && git push --follow-tags && npm publish"
  }
}
